rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: DEVELOPMENT ONLY - These rules are insecure and allow open access.
    // This is a temporary workaround for an external configuration issue.
    match /{path=**} {
      allow read, write: if true;
    }
  }
}
